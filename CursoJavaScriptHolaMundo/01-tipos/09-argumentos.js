function suma(a){ // cuando referenciamos los valores dentro de las funciones se llaman parametros.
    return a + 2;
}

let resultado = suma(5); //los valores que le pasamos a las funciones cuando las llamamos se llaman argumentos, va a dar 7 el resultado
console.log(resultado);


/* pueden recibir varios valores dentro de los parentesis, por ejemplo*/
// function suma(a, b){ return a + b} 

//let resultado = suma (5, 6)